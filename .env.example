# -------------------------------------------------------
# .env.example  –  FacAutos.com (Angular 8)
# Copy to .env and adjust values for your environment.
# -------------------------------------------------------

# Node / Angular-CLI
NODE_ENV=development
PORT=4200

# Production build flag (used by Angular CLI --configuration)
# Set to "production" when building for prod.
CONFIGURATION=development
